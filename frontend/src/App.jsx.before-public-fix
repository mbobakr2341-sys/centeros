import PublicSite from "./PublicSite";
import { useEffect, useRef, useState } from 'react'
import './App.css'

const translations = {
  en: {
    welcome: 'Welcome back',
    subtitle: 'Sign in to continue to your CenterOS workspace.',
    centerCode: 'Center Code',
    centerPlaceholder: 'Enter your center code',
    email: 'Email address',
    emailPlaceholder: 'Enter your email address',
    password: 'Password',
    passwordPlaceholder: 'Enter your password',
    signIn: 'Sign In',
    signingIn: 'Signing in...',
    language: 'Language',
    english: 'English',
    turkish: 'Türkçe',
    arabic: 'العربية',
    footer: 'CenterOS Management Platform',
  },
  tr: {
    welcome: 'Tekrar hoş geldiniz',
    subtitle: 'CenterOS çalışma alanınıza devam etmek için giriş yapın.',
    centerCode: 'Merkez Kodu',
    centerPlaceholder: 'Merkez kodunuzu girin',
    email: 'E-posta adresi',
    emailPlaceholder: 'E-posta adresinizi girin',
    password: 'Şifre',
    passwordPlaceholder: 'Şifrenizi girin',
    signIn: 'Giriş Yap',
    signingIn: 'Giriş yapılıyor...',
    language: 'Dil',
    english: 'English',
    turkish: 'Türkçe',
    arabic: 'العربية',
    footer: 'CenterOS Yönetim Platformu',
  },
  ar: {
    welcome: 'مرحبًا بعودتك',
    subtitle: 'سجّل الدخول للمتابعة إلى مساحة العمل الخاصة بك في CenterOS.',
    centerCode: 'كود المركز',
    centerPlaceholder: 'اكتب كود المركز',
    email: 'البريد الإلكتروني',
    emailPlaceholder: 'اكتب بريدك الإلكتروني',
    password: 'كلمة المرور',
    passwordPlaceholder: 'اكتب كلمة المرور',
    signIn: 'تسجيل الدخول',
    signingIn: 'جارٍ تسجيل الدخول...',
    language: 'اللغة',
    english: 'English',
    turkish: 'Türkçe',
    arabic: 'العربية',
    footer: 'منصة CenterOS لإدارة المراكز',
  },
}

function Icon({ name, size = 20 }) {
  const props = {
    width: size,
    height: size,
    viewBox: '0 0 24 24',
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: 1.8,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    'aria-hidden': true,
  }

  const icons = {
    globe: (
      <>
        <circle cx="12" cy="12" r="9" />
        <path d="M3 12h18" />
        <path d="M12 3c2.5 2.6 3.6 5.6 3.6 9S14.5 18.4 12 21" />
        <path d="M12 3c-2.5 2.6-3.6 5.6-3.6 9S9.5 18.4 12 21" />
      </>
    ),
    building: (
      <>
        <path d="M4 21V5.5L12 3l8 2.5V21" />
        <path d="M8 8h1" />
        <path d="M15 8h1" />
        <path d="M8 12h1" />
        <path d="M15 12h1" />
        <path d="M8 16h1" />
        <path d="M15 16h1" />
        <path d="M10 21v-3h4v3" />
      </>
    ),
    mail: (
      <>
        <rect x="3" y="5" width="18" height="14" rx="2" />
        <path d="m3 7 9 6 9-6" />
      </>
    ),
    lock: (
      <>
        <rect x="5" y="10" width="14" height="11" rx="2" />
        <path d="M8 10V7a4 4 0 0 1 8 0v3" />
      </>
    ),
    eye: (
      <>
        <path d="M2.5 12s3.5-6 9.5-6 9.5 6 9.5 6-3.5 6-9.5 6-9.5-6-9.5-6Z" />
        <circle cx="12" cy="12" r="2.5" />
      </>
    ),
    eyeOff: (
      <>
        <path d="m3 3 18 18" />
        <path d="M10.6 6.2A10.8 10.8 0 0 1 12 6c6 0 9.5 6 9.5 6a18 18 0 0 1-3 3.5" />
        <path d="M6.7 6.8C4.1 8.5 2.5 12 2.5 12s3.5 6 9.5 6c1 0 1.9-.2 2.8-.5" />
        <path d="M9.9 9.9a3 3 0 0 0 4.2 4.2" />
      </>
    ),
    arrow: (
      <>
        <path d="M5 12h14" />
        <path d="m13 6 6 6-6 6" />
      </>
    ),
    check: <path d="m5 12 4 4L19 6" />,
  }

  return <svg {...props}>{icons[name]}</svg>
}

function LanguageSelector({ language, setLanguage }) {
  const [open, setOpen] = useState(false)
  const ref = useRef(null)

  const languages = [
    { code: 'en', short: 'EN', label: 'English' },
    { code: 'tr', short: 'TR', label: 'Türkçe' },
    { code: 'ar', short: 'AR', label: 'العربية' },
  ]

  const current =
    languages.find((item) => item.code === language) ||
    languages[0]

  useEffect(() => {
    const handleClick = (event) => {
      if (ref.current && !ref.current.contains(event.target)) {
        setOpen(false)
      }
    }

    document.addEventListener('mousedown', handleClick)

    return () => {
      document.removeEventListener('mousedown', handleClick)
    }
  }, [])

  return (
    <div className="language-selector" ref={ref}>
      <button
        type="button"
        className={`language-trigger ${open ? 'is-open' : ''}`}
        onClick={() => setOpen((value) => !value)}
        aria-expanded={open}
      >
        <span className="language-trigger-icon">
          <Icon name="globe" size={17} />
        </span>

        <span>{current.short}</span>

        <span className="language-chevron">
          <svg
            width="13"
            height="13"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="2"
            strokeLinecap="round"
            strokeLinejoin="round"
          >
            <path d="m6 9 6 6 6-6" />
          </svg>
        </span>
      </button>

      {open && (
        <div className="language-menu">
          <div className="language-menu-header">
            Language
          </div>

          {languages.map((item) => (
            <button
              type="button"
              key={item.code}
              className={`language-option ${
                language === item.code ? 'selected' : ''
              }`}
              onClick={() => {
                setLanguage(item.code)
                setOpen(false)
              }}
            >
              <span className="language-code">
                {item.short}
              </span>

              <span className="language-label">
                {item.label}
              </span>

              {language === item.code && (
                <span className="language-selected">
                  <Icon name="check" size={14} />
                </span>
              )}
            </button>
          ))}
        </div>
      )}
    </div>
  )
}

function Login({ language, setLanguage, onLogin }) {
  const t = translations[language]

  const [centerCode, setCenterCode] = useState('')
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [showPassword, setShowPassword] = useState(false)
  const [loading, setLoading] = useState(false)

  const handleSubmit = (event) => {
    event.preventDefault()

    if (!centerCode.trim() || !email.trim() || !password) {
      return
    }

    setLoading(true)

    setTimeout(() => {
      setLoading(false)
      onLogin()
    }, 700)
  }

  return (
    <main
      className={`login-page ${
        language === 'ar' ? 'rtl' : ''
      }`}
    >
      <div className="login-background">
        <div className="background-grid" />
        <div className="background-orb orb-one" />
        <div className="background-orb orb-two" />
        <div className="background-orb orb-three" />
      </div>

      <header className="login-topbar">
        <LanguageSelector
          language={language}
          setLanguage={setLanguage}
        />
      </header>

      <section className="login-card">
        <div className="login-card-glow" />

        <div className="brand">
          <div className="brand-logo-container">
            <img
              src="/logo.png"
              alt="CenterOS"
              className="brand-logo"
            />
          </div>

          <div className="brand-name">
            CenterOS
          </div>

          <div className="brand-line">
            Management Platform
          </div>
        </div>

        <div className="login-heading">
          <h1>{t.welcome}</h1>
          <p>{t.subtitle}</p>
        </div>

        <form
          className="login-form"
          onSubmit={handleSubmit}
        >
          <div className="form-field">
            <label htmlFor="center-code">
              {t.centerCode}
            </label>

            <div className="input-wrapper">
              <span className="input-icon">
                <Icon name="building" size={19} />
              </span>

              <input
                id="center-code"
                type="text"
                value={centerCode}
                onChange={(event) =>
                  setCenterCode(event.target.value)
                }
                placeholder={t.centerPlaceholder}
                autoComplete="organization"
                spellCheck="false"
                required
              />
            </div>
          </div>

          <div className="form-field">
            <label htmlFor="email">
              {t.email}
            </label>

            <div className="input-wrapper">
              <span className="input-icon">
                <Icon name="mail" size={19} />
              </span>

              <input
                id="email"
                type="email"
                value={email}
                onChange={(event) =>
                  setEmail(event.target.value)
                }
                placeholder={t.emailPlaceholder}
                autoComplete="email"
                required
              />
            </div>
          </div>

          <div className="form-field">
            <label htmlFor="password">
              {t.password}
            </label>

            <div className="input-wrapper password-wrapper">
              <span className="input-icon">
                <Icon name="lock" size={19} />
              </span>

              <input
                id="password"
                type={showPassword ? 'text' : 'password'}
                value={password}
                onChange={(event) =>
                  setPassword(event.target.value)
                }
                placeholder={t.passwordPlaceholder}
                autoComplete="current-password"
                required
              />

              <button
                type="button"
                className="password-toggle"
                onClick={() =>
                  setShowPassword((value) => !value)
                }
              >
                <Icon
                  name={showPassword ? 'eyeOff' : 'eye'}
                  size={19}
                />
              </button>
            </div>
          </div>

          <button
            type="submit"
            className="sign-in-button"
            disabled={loading}
          >
            <span>
              {loading ? t.signingIn : t.signIn}
            </span>

            {!loading && (
              <span className="button-arrow">
                <Icon name="arrow" size={18} />
              </span>
            )}

            {loading && (
              <span className="button-loader" />
            )}
          </button>
        </form>

        <div className="login-footer">
          {t.footer}
        </div>
      </section>

      <div className="login-bottom">
        <span>
          © {new Date().getFullYear()} CenterOS
        </span>

        <span className="bottom-separator" />

        <span>All rights reserved</span>
      </div>
    </main>
  )
}

function Dashboard({ language, onLogout }) {
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false)
  const [activePage, setActivePage] = useState('dashboard')

  const isArabic = language === 'ar'

  const navItems = [
    { id: 'dashboard', label: isArabic ? 'لوحة التحكم' : language === 'tr' ? 'Kontrol Paneli' : 'Dashboard', icon: 'grid' },
    { id: 'centers', label: isArabic ? 'المراكز' : language === 'tr' ? 'Merkezler' : 'Centers', icon: 'building' },
    { id: 'users', label: isArabic ? 'المستخدمون' : language === 'tr' ? 'Kullanıcılar' : 'Users', icon: 'users' },
    { id: 'doctors', label: isArabic ? 'الأطباء' : language === 'tr' ? 'Doktorlar' : 'Doctors', icon: 'doctor' },
    { id: 'patients', label: isArabic ? 'المرضى' : language === 'tr' ? 'Hastalar' : 'Patients', icon: 'patients' },
    { id: 'bookings', label: isArabic ? 'الحجوزات' : language === 'tr' ? 'Randevular' : 'Bookings', icon: 'calendar' },
    { id: 'branches', label: isArabic ? 'العيادات' : language === 'tr' ? 'Klinikler' : 'Clinics', icon: 'clinic' },
    { id: 'services', label: isArabic ? 'الخدمات' : language === 'tr' ? 'Hizmetler' : 'Services', icon: 'tag' },
    { id: 'invoices', label: isArabic ? 'الفواتير' : language === 'tr' ? 'Faturalar' : 'Invoices', icon: 'invoice' },
    { id: 'reports', label: isArabic ? 'التقارير' : language === 'tr' ? 'Raporlar' : 'Reports', icon: 'chart' },
    { id: 'settings', label: isArabic ? 'الإعدادات' : language === 'tr' ? 'Ayarlar' : 'Settings', icon: 'settings' },
  ]

  const stats = [
    {
      title: isArabic ? 'إجمالي الإيرادات' : language === 'tr' ? 'Toplam Gelir' : 'Total Revenue',
      value: '0',
      change: null,
      icon: 'money',
      type: 'purple',
    },
    {
      title: isArabic ? 'إجمالي الأطباء' : language === 'tr' ? 'Toplam Doktor' : 'Total Doctors',
      value: '0',
      change: null,
      icon: 'doctor',
      type: 'cyan',
    },
    {
      title: isArabic ? 'إجمالي الحجوزات' : language === 'tr' ? 'Toplam Randevu' : 'Total Bookings',
      value: '0',
      change: null,
      icon: 'calendar',
      type: 'blue',
    },
    {
      title: isArabic ? 'إجمالي المرضى' : language === 'tr' ? 'Toplam Hasta' : 'Total Patients',
      value: '0',
      change: null,
      icon: 'patients',
      type: 'violet',
    },
  ]

  const bookings = []

  const chartData = []

  const chartPoints = chartData
    .map((value, index) => {
      const x = 30 + index * 65
      const y = 190 - (value / 450) * 150
      return `${x},${y}`
    })
    .join(' ')

  const serviceData = []

  const revenueData = []

  const DashboardIcon = ({ name, size = 20 }) => {
    const common = {
      width: size,
      height: size,
      viewBox: '0 0 24 24',
      fill: 'none',
      stroke: 'currentColor',
      strokeWidth: 1.8,
      strokeLinecap: 'round',
      strokeLinejoin: 'round',
    }

    const icons = {
      grid: (
        <>
          <rect x="3" y="3" width="7" height="7" rx="1.5" />
          <rect x="14" y="3" width="7" height="7" rx="1.5" />
          <rect x="3" y="14" width="7" height="7" rx="1.5" />
          <rect x="14" y="14" width="7" height="7" rx="1.5" />
        </>
      ),
      building: (
        <>
          <path d="M4 21V5l8-2 8 2v16" />
          <path d="M8 8h1" />
          <path d="M15 8h1" />
          <path d="M8 12h1" />
          <path d="M15 12h1" />
          <path d="M8 16h1" />
          <path d="M15 16h1" />
          <path d="M10 21v-3h4v3" />
        </>
      ),
      users: (
        <>
          <circle cx="9" cy="8" r="3" />
          <path d="M3.5 20c.6-3.3 2.5-5 5.5-5s4.9 1.7 5.5 5" />
          <path d="M16 5.5a3 3 0 0 1 0 5.8" />
          <path d="M17 15c2.1.3 3.5 1.8 4 4" />
        </>
      ),
      doctor: (
        <>
          <circle cx="12" cy="7" r="3.5" />
          <path d="M5 21c.7-4 3-6 7-6s6.3 2 7 6" />
          <path d="M18 4v5" />
          <path d="M15.5 6.5h5" />
        </>
      ),
      patients: (
        <>
          <circle cx="12" cy="8" r="3" />
          <path d="M5 21c.6-3.8 2.9-6 7-6s6.4 2.2 7 6" />
          <path d="M5 11a3 3 0 0 1-1-5.8" />
          <path d="M19 11a3 3 0 0 0 1-5.8" />
        </>
      ),
      calendar: (
        <>
          <rect x="3" y="5" width="18" height="16" rx="2" />
          <path d="M7 3v4" />
          <path d="M17 3v4" />
          <path d="M3 10h18" />
          <path d="M8 14h3" />
          <path d="M8 17h5" />
        </>
      ),
      clinic: (
        <>
          <path d="M4 21V5h16v16" />
          <path d="M8 8h2v3H8z" />
          <path d="M14 8h2v3h-2z" />
          <path d="M8 14h2v3H8z" />
          <path d="M14 14h2v3h-2z" />
          <path d="M10 21v-4h4v4" />
        </>
      ),
      tag: (
        <>
          <path d="m20 13-7 7-10-10V3h7l10 10Z" />
          <circle cx="7.5" cy="7.5" r="1.2" />
        </>
      ),
      invoice: (
        <>
          <path d="M6 3h12v18l-3-2-3 2-3-2-3 2V3Z" />
          <path d="M9 8h6" />
          <path d="M9 12h6" />
          <path d="M9 16h4" />
        </>
      ),
      chart: (
        <>
          <path d="M4 19V5" />
          <path d="M4 19h17" />
          <path d="m7 15 4-5 3 2 5-7" />
        </>
      ),
      settings: (
        <>
          <circle cx="12" cy="12" r="3" />
          <path d="M19.4 15a1.7 1.7 0 0 0 .3 1.9l.1.1-1.7 1.7-.1-.1a1.7 1.7 0 0 0-1.9-.3 1.7 1.7 0 0 0-1 1.6V20h-2.4v-.1a1.7 1.7 0 0 0-1-1.6 1.7 1.7 0 0 0-1.9.3l-.1.1L8 17l.1-.1a1.7 1.7 0 0 0 .3-1.9 1.7 1.7 0 0 0-1.6-1H6v-2.4h.8a1.7 1.7 0 0 0 1.6-1 1.7 1.7 0 0 0-.3-1.9L8 8.6l1.7-1.7.1.1a1.7 1.7 0 0 0 1.9.3 1.7 1.7 0 0 0 1-1.6V5h2.4v.7a1.7 1.7 0 0 0 1 1.6 1.7 1.7 0 0 0 1.9-.3l.1-.1L19.8 8l-.1.1a1.7 1.7 0 0 0-.3 1.9 1.7 1.7 0 0 0 1.6 1h.7v2.4H21a1.7 1.7 0 0 0-1.6 1Z" />
        </>
      ),
      money: (
        <>
          <circle cx="12" cy="12" r="9" />
          <path d="M14.5 9.5c-.5-1-1.3-1.5-2.6-1.5-1.5 0-2.5.8-2.5 1.8 0 2.8 5.2 1.1 5.2 3.9 0 1.1-1 2-2.7 2-1.4 0-2.3-.5-2.9-1.5" />
          <path d="M12 6.5v11" />
        </>
      ),
      search: (
        <>
          <circle cx="10.8" cy="10.8" r="6.8" />
          <path d="m16 16 5 5" />
        </>
      ),
      bell: (
        <>
          <path d="M18 9a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9" />
          <path d="M10 21h4" />
        </>
      ),
      moon: (
        <path d="M20 15.5A8 8 0 0 1 8.5 4 8.5 8.5 0 1 0 20 15.5Z" />
      ),
      globe: (
        <>
          <circle cx="12" cy="12" r="9" />
          <path d="M3 12h18" />
          <path d="M12 3c2.5 2.6 3.5 5.7 3.5 9s-1 6.4-3.5 9" />
          <path d="M12 3c-2.5 2.6-3.5 5.7-3.5 9s1 6.4 3.5 9" />
        </>
      ),
      menu: (
        <>
          <path d="M4 7h16" />
          <path d="M4 12h16" />
          <path d="M4 17h16" />
        </>
      ),
      arrow: (
        <>
          <path d="M5 12h14" />
          <path d="m13 6 6 6-6 6" />
        </>
      ),
    }

    return <svg {...common}>{icons[name] || icons.grid}</svg>
  }

  return (
    <div
      className={`dashboard-shell ${
        isArabic ? 'rtl' : ''
      }`}
    >
      <aside
        className={`dashboard-sidebar ${
          mobileMenuOpen ? 'mobile-open' : ''
        }`}
      >
        <div className="sidebar-brand">
          <div className="sidebar-logo">
            <img src="/logo.png" alt="CenterOS" />
          </div>

          <div>
            <div className="sidebar-brand-name">
              CenterOS
            </div>
            <div className="sidebar-brand-subtitle">
              Management Platform
            </div>
          </div>
        </div>

        <nav className="sidebar-nav">
          {navItems.map((item) => (
            <button
              type="button"
              key={item.id}
              className={`sidebar-nav-item ${
                activePage === item.id ? 'active' : ''
              }`}
              onClick={() => {
                setActivePage(item.id)
                setMobileMenuOpen(false)
              }}
            >
              <span className="sidebar-nav-icon">
                <DashboardIcon
                  name={item.icon}
                  size={19}
                />
              </span>

              <span>{item.label}</span>
            </button>
          ))}
        </nav>

        <div className="sidebar-profile">
          <div className="profile-avatar">
            AD
            <span className="online-dot" />
          </div>

          <div className="profile-info">
            <strong>Admin</strong>
            <span>
              {isArabic
                ? 'مدير النظام'
                : language === 'tr'
                  ? 'Süper Yönetici'
                  : 'Super Administrator'}
            </span>
          </div>

          <button
            type="button"
            className="profile-menu-button"
            onClick={onLogout}
            title="Logout"
          >
            <span>⌄</span>
          </button>
        </div>
      </aside>

      {mobileMenuOpen && (
        <button
          type="button"
          className="dashboard-overlay"
          onClick={() => setMobileMenuOpen(false)}
          aria-label="Close menu"
        />
      )}

      <section className="dashboard-main">
        <header className="dashboard-topbar">
          <div className="topbar-left">
            <button
              type="button"
              className="mobile-menu-button"
              onClick={() =>
                setMobileMenuOpen(true)
              }
            >
              <DashboardIcon
                name="menu"
                size={21}
              />
            </button>

            <div className="dashboard-search">
              <DashboardIcon
                name="search"
                size={20}
              />

              <input
                type="search"
                placeholder={
                  isArabic
                    ? 'ابحث هنا...'
                    : language === 'tr'
                      ? 'Burada ara...'
                      : 'Search here...'
                }
              />

              <span className="search-shortcut">
                ⌘ K
              </span>
            </div>
          </div>

          <div className="topbar-actions">
            <button
              type="button"
              className="topbar-icon-button"
              title="Theme"
            >
              <DashboardIcon
                name="moon"
                size={20}
              />
            </button>

            <button
              type="button"
              className="topbar-icon-button notification-button"
              title="Notifications"
            >
              <DashboardIcon
                name="bell"
                size={20}
              />
              <span className="notification-count">
                5
              </span>
            </button>

            <button
              type="button"
              className="topbar-icon-button"
              title="Language"
            >
              <DashboardIcon
                name="globe"
                size={20}
              />
            </button>

            <div className="center-selector">
              <div className="center-selector-text">
                <strong>
                  {isArabic
                    ? 'مركز النخبة الطبي'
                    : language === 'tr'
                      ? 'Elite Medical Center'
                      : 'Elite Medical Center'}
                </strong>

                <span>CENTER-001</span>
              </div>

              <span className="center-chevron">
                ⌄
              </span>

              <div className="center-selector-icon">
                <DashboardIcon
                  name="building"
                  size={19}
                />
              </div>
            </div>
          </div>
        </header>

        {activePage === 'dashboard' ? (
          <div className="dashboard-content">
            <div className="dashboard-heading">
              <div>
                <h1>
                  {isArabic
                    ? 'لوحة التحكم'
                    : language === 'tr'
                      ? 'Kontrol Paneli'
                      : 'Dashboard'}
                </h1>

                <p>
                  {isArabic
                    ? 'مرحبًا بك في نظام إدارة المركز الطبي'
                    : language === 'tr'
                      ? 'Merkez yönetim sisteminize hoş geldiniz'
                      : 'Welcome back to your center management system'}
                </p>
              </div>

              <button
                type="button"
                className="date-filter"
              >
                <DashboardIcon
                  name="calendar"
                  size={18}
                />

                <span>
                  {isArabic
                    ? '31 مايو 2024'
                    : language === 'tr'
                      ? '31 Mayıs 2024'
                      : 'May 31, 2024'}
                </span>

                <span className="date-chevron">
                  ⌄
                </span>
              </button>
            </div>

            <div className="stats-grid">
              {stats.map((stat) => (
                <div
                  className="stat-card"
                  key={stat.title}
                >
                  <div
                    className={`stat-icon ${stat.type}`}
                  >
                    <DashboardIcon
                      name={stat.icon}
                      size={23}
                    />
                  </div>

                  <div className="stat-data">
                    <span className="stat-title">
                      {stat.title}
                    </span>

                    <strong className="stat-value">
                      {stat.value}
                    </strong>

                    <span className="stat-change">
                      {stat.change}
                      <small> ↗</small>
                    </span>
                  </div>

                  <span className="stat-period">
                    {isArabic
                      ? 'من الشهر الماضي'
                      : language === 'tr'
                        ? 'Geçen aydan'
                        : 'from last month'}
                  </span>
                </div>
              ))}
            </div>

            <div className="dashboard-grid">
              <div className="dashboard-left-column">
                <section className="panel bookings-chart-panel">
                  <div className="panel-header">
                    <div>
                      <h2>
                        {isArabic
                          ? 'نظرة عامة على الحجوزات'
                          : language === 'tr'
                            ? 'Randevu Genel Bakış'
                            : 'Bookings Overview'}
                      </h2>
                    </div>

                    <button
                      type="button"
                      className="panel-select"
                    >
                      {isArabic
                        ? 'الحجوزات'
                        : language === 'tr'
                          ? 'Randevular'
                          : 'Bookings'}
                      <span>⌄</span>
                    </button>
                  </div>

                  <div className="line-chart">
                    <div className="chart-y-axis">
                      <span>500</span>
                      <span>400</span>
                      <span>300</span>
                      <span>200</span>
                      <span>100</span>
                      <span>0</span>
                    </div>

                    <div className="chart-area">
                      <div className="chart-grid-lines">
                        <span />
                        <span />
                        <span />
                        <span />
                        <span />
                        <span />
                      </div>

                      <svg
                        className="chart-svg"
                        viewBox="0 0 820 220"
                        preserveAspectRatio="none"
                      >
                        <defs>
                          <linearGradient
                            id="chartFill"
                            x1="0"
                            y1="0"
                            x2="0"
                            y2="1"
                          >
                            <stop
                              offset="0%"
                              stopColor="#397ff5"
                              stopOpacity="0.26"
                            />
                            <stop
                              offset="100%"
                              stopColor="#397ff5"
                              stopOpacity="0"
                            />
                          </linearGradient>
                        </defs>

                        <polyline
                          points={`30,190 ${chartPoints}`}
                          fill="none"
                          stroke="#3b82f6"
                          strokeWidth="3"
                          strokeLinecap="round"
                          strokeLinejoin="round"
                        />

                        <polygon
                          points={`30,190 ${chartPoints} 810,190`}
                          fill="url(#chartFill)"
                        />

                        {chartData.map(
                          (value, index) => {
                            const x =
                              30 + index * 65
                            const y =
                              190 -
                              (value / 450) * 150

                            return (
                              <circle
                                key={index}
                                cx={x}
                                cy={y}
                                r="4"
                                fill="#0b1425"
                                stroke="#5b96ff"
                                strokeWidth="2"
                              />
                            )
                          }
                        )}
                      </svg>

                      <div className="chart-x-axis">
                        <span>01 May</span>
                        <span>06 May</span>
                        <span>11 May</span>
                        <span>16 May</span>
                        <span>21 May</span>
                        <span>26 May</span>
                        <span>31 May</span>
                      </div>
                    </div>
                  </div>
                </section>

                <div className="bottom-panels">
                  <section className="panel services-panel">
                    <div className="panel-header">
                      <h2>
                        {isArabic
                          ? 'توزيع الخدمات'
                          : language === 'tr'
                            ? 'Hizmet Dağılımı'
                            : 'Services Distribution'}
                      </h2>
                    </div>

                    <div className="services-content">
                      <div className="donut-chart">
                        <div className="donut-inner">
                          <strong>1,752</strong>
                          <span>
                            {isArabic
                              ? 'الإجمالي'
                              : language === 'tr'
                                ? 'Toplam'
                                : 'Total'}
                          </span>
                        </div>
                      </div>

                      <div className="services-list">
                        {serviceData.map(
                          (item) => (
                            <div
                              className="service-row"
                              key={item.label}
                            >
                              <span
                                className={`service-dot ${item.color}`}
                              />

                              <span className="service-name">
                                {item.label}
                              </span>

                              <strong>
                                {item.value}
                              </strong>
                            </div>
                          )
                        )}
                      </div>
                    </div>
                  </section>

                  <section className="panel revenue-panel">
                    <div className="panel-header">
                      <h2>
                        {isArabic
                          ? 'الإيرادات الشهرية'
                          : language === 'tr'
                            ? 'Aylık Gelir'
                            : 'Monthly Revenue'}
                      </h2>

                      <button
                        type="button"
                        className="panel-select"
                      >
                        {isArabic
                          ? 'هذا الشهر'
                          : language === 'tr'
                            ? 'Bu ay'
                            : 'This month'}
                        <span>⌄</span>
                      </button>
                    </div>

                    <div className="bar-chart">
                      <div className="bar-y-axis">
                        <span>100K</span>
                        <span>80K</span>
                        <span>60K</span>
                        <span>40K</span>
                        <span>20K</span>
                        <span>0</span>
                      </div>

                      <div className="bars-area">
                        <div className="bar-lines">
                          <span />
                          <span />
                          <span />
                          <span />
                          <span />
                        </div>

                        <div className="bars">
                          {revenueData.map(
                            (value, index) => (
                              <div
                                className="bar-column"
                                key={index}
                              >
                                <div
                                  className={`bar ${
                                    index ===
                                    revenueData.length -
                                      1
                                      ? 'current'
                                      : ''
                                  }`}
                                  style={{
                                    height: `${value}%`,
                                  }}
                                />

                                <span>
                                  {isArabic
                                    ? `الأسبوع ${index + 1}`
                                    : language === 'tr'
                                      ? `${index + 1}. Hafta`
                                      : `Week ${index + 1}`}
                                </span>
                              </div>
                            )
                          )}
                        </div>
                      </div>
                    </div>
                  </section>
                </div>
              </div>

              <div className="dashboard-right-column">
                <section className="panel recent-panel">
                  <div className="panel-header">
                    <h2>
                      {isArabic
                        ? 'الحجوزات الحديثة'
                        : language === 'tr'
                          ? 'Son Randevular'
                          : 'Recent Bookings'}
                    </h2>

                    <button
                      type="button"
                      className="view-all-button"
                    >
                      {isArabic
                        ? 'عرض الكل'
                        : language === 'tr'
                          ? 'Tümünü Gör'
                          : 'View all'}
                    </button>
                  </div>

                  <div className="booking-list">
                    {bookings.map(
                      (booking) => (
                        <div
                          className="booking-item"
                          key={`${booking.name}-${booking.time}`}
                        >
                          <div
                            className={`booking-avatar ${booking.color}`}
                          >
                            {booking.initials}
                          </div>

                          <div className="booking-person">
                            <strong>
                              {booking.name}
                            </strong>
                            <span>
                              {booking.service}
                            </span>
                          </div>

                          <span
                            className={`booking-status ${booking.status}`}
                          >
                            {booking.status ===
                            'confirmed'
                              ? isArabic
                                ? 'مؤكد'
                                : language === 'tr'
                                  ? 'Onaylandı'
                                  : 'Confirmed'
                              : isArabic
                                ? 'معلق'
                                : language === 'tr'
                                  ? 'Bekliyor'
                                  : 'Pending'}
                          </span>

                          <span className="booking-time">
                            {booking.time}
                          </span>
                        </div>
                      )
                    )}
                  </div>

                  <button
                    type="button"
                    className="full-bookings-button"
                  >
                    <span>
                      {isArabic
                        ? 'عرض جميع الحجوزات'
                        : language === 'tr'
                          ? 'Tüm randevuları gör'
                          : 'View all bookings'}
                    </span>

                    <DashboardIcon
                      name="calendar"
                      size={16}
                    />
                  </button>
                </section>

                <section className="panel quick-panel">
                  <div className="panel-header">
                    <h2>
                      {isArabic
                        ? 'ملخص سريع'
                        : language === 'tr'
                          ? 'Hızlı Özet'
                          : 'Quick Summary'}
                    </h2>
                  </div>

                  <div className="quick-grid">
                    <div className="quick-card">
                      <div className="quick-card-icon purple">
                        <DashboardIcon
                          name="chart"
                          size={19}
                        />
                      </div>

                      <span>
                        {isArabic
                          ? 'معدل إلغاء الحجوزات'
                          : language === 'tr'
                            ? 'İptal Oranı'
                            : 'Cancellation Rate'}
                      </span>

                      <strong>2.4%</strong>

                      <small className="positive">
                        −1.3% ↓
                      </small>
                    </div>

                    <div className="quick-card">
                      <div className="quick-card-icon blue">
                        <DashboardIcon
                          name="calendar"
                          size={19}
                        />
                      </div>

                      <span>
                        {isArabic
                          ? 'متوسط الحجز اليومي'
                          : language === 'tr'
                            ? 'Günlük Ortalama'
                            : 'Daily Avg. Bookings'}
                      </span>

                      <strong>56</strong>

                      <small className="positive">
                        +9.2%
                      </small>
                    </div>

                    <div className="quick-card">
                      <div className="quick-card-icon cyan">
                        <DashboardIcon
                          name="building"
                          size={19}
                        />
                      </div>

                      <span>
                        {isArabic
                          ? 'إجمالي المراكز'
                          : language === 'tr'
                            ? 'Toplam Merkez'
                            : 'Total Centers'}
                      </span>

                      <strong>12</strong>
                    </div>

                    <div className="quick-card">
                      <div className="quick-card-icon violet">
                        <DashboardIcon
                          name="users"
                          size={19}
                        />
                      </div>

                      <span>
                        {isArabic
                          ? 'عدد الموظفين'
                          : language === 'tr'
                            ? 'Çalışan Sayısı'
                            : 'Employees'}
                      </span>

                      <strong>96</strong>
                    </div>
                  </div>
                </section>
              </div>
            </div>

            <footer className="dashboard-footer">
              © 2024 <span>CenterOS</span>.{' '}
              {isArabic
                ? 'جميع الحقوق محفوظة.'
                : language === 'tr'
                  ? 'Tüm hakları saklıdır.'
                  : 'All rights reserved.'}
            </footer>
          </div>
        ) : (
          <div className="dashboard-empty-state">
            <div className="dashboard-empty-icon">
              <DashboardIcon
                name={
                  navItems.find(
                    (item) =>
                      item.id === activePage
                  )?.icon || 'grid'
                }
                size={32}
              />
            </div>

            <h2>
              {
                navItems.find(
                  (item) =>
                    item.id === activePage
                )?.label
              }
            </h2>

            <p>
              {isArabic
                ? 'هذه الصفحة سيتم تطويرها في المرحلة التالية.'
                : language === 'tr'
                  ? 'Bu bölüm sonraki aşamada geliştirilecek.'
                  : 'This section will be built in the next stage.'}
            </p>
          </div>
        )}
      </section>
    </div>
  )
}

function InternalApp() {
  const [language, setLanguage] = useState(() => {
    return localStorage.getItem('centeros_language') || 'en'
  })

  const [view, setView] = useState('login')

  useEffect(() => {
    localStorage.setItem(
      'centeros_language',
      language
    )

    document.documentElement.lang = language
    document.documentElement.dir =
      language === 'ar' ? 'rtl' : 'ltr'
  }, [language])

  return view === 'login' ? (
    <Login
      language={language}
      setLanguage={setLanguage}
      onLogin={() => setView('dashboard')}
    />
  ) : (
    <Dashboard
      language={language}
      onLogout={() => setView('login')}
    />
  )
}




function PublicEntryPoint() {
  return <PublicSite />;
}

export default PublicEntryPoint;
